# Government Institution Server Configuration
# Company WAS + Company PostgreSQL Server

# Database - Company PostgreSQL Server
DB_HOST=192.168.1.50
DB_PORT=5432
DB_USER=katc1_gov
DB_PASSWORD=your_secure_password_changed_by_admin
DB_NAME=katc1_auth_gov

# JWT Secret - Production (Must be different from AWS)
JWT_SECRET=generate_with_openssl_rand_-base64_32

# App Configuration
NODE_ENV=production
NEXT_PUBLIC_APP_NAME=KATC 유사호출부호 경고시스템

# Company Server Configuration
COMPANY_WAS_HOST=192.168.1.100
COMPANY_WAS_PORT=8080
COMPANY_NETWORK_SECURITY=true

# Proxy Settings (if behind company firewall)
HTTP_PROXY=http://proxy.company.com:3128
HTTPS_PROXY=http://proxy.company.com:3128
NO_PROXY=localhost,127.0.0.1,192.168.1.0/24

# Email (company mail server)
SMTP_HOST=mail.company.com
SMTP_PORT=25
SMTP_USER=no-reply@company.com
SMTP_PASSWORD=
SMTP_FROM=no-reply@company.com

# Security
ENABLE_SSL_VERIFICATION=true
ALLOWED_DOMAINS=katc.company.com,*.company.com
