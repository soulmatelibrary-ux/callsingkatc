# ============================================
# KATC1 유사호출부호 경고시스템 - 환경 설정
# ============================================
# .env.local 파일을 생성하여 아래 값들을 설정하세요
# 주의: .env.local은 .gitignore에 추가되어 커밋되지 않습니다

# ============================================
# 🔑 bkend.ai Configuration (필수)
# ============================================
# https://bkend.ai에서 프로젝트 생성 후 Project ID 입력
# Render 환경 변수로 설정하세요
NEXT_PUBLIC_BKEND_PROJECT_ID=your_bkend_project_id

# ============================================
# 📊 Database Configuration (SQLite)
# ============================================
DB_PATH=./data/katc1.db

# ============================================
# 🔐 JWT Secret
# ============================================
# 프로덕션: openssl로 생성하여 Render 환경 변수로 설정
# 생성 방법 (터미널):
# openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_generated_with_openssl

# ============================================
# 🌐 Application Configuration
# ============================================
NODE_ENV=production
NEXT_PUBLIC_APP_NAME=KATC 유사호출부호 경고시스템

# ============================================
# 📧 Email (SMTP) Configuration
# ============================================
# 📌 현재 사용: Gmail (개발)
# 📅 예정: 회사 메일서버 (추후 변경)

# 옵션 1: AWS SES (프로덕션)
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=your-ses-user
# SMTP_PASSWORD=your-ses-password

# ✅ 옵션 2: Gmail (현재 사용 중)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here
SMTP_FROM_EMAIL=your-email@gmail.com

# 🔜 옵션 3: 회사 메일서버 (다음 단계에서 사용 예정)
# SMTP_HOST=mail.company.com
# SMTP_PORT=25
# SMTP_USER=no-reply@company.com
# SMTP_PASSWORD=
# SMTP_FROM=no-reply@company.com

# ============================================
# 🚀 선택사항 (프로덕션)
# ============================================
# NEXT_PUBLIC_APP_URL=http://localhost:3000
# LOG_LEVEL=info
# AWS_REGION=us-east-1
# NEXT_PUBLIC_CDN_URL=https://your-cdn-domain.com

# ============================================
# 보안 주의사항
# ============================================
# 1. 민감한 정보는 .env.local에만 저장 (gitignore)
# 2. .env.example은 기본값만 포함 (프로젝트에 커밋)
# 3. 프로덕션에서는 환경변수로 직접 설정
# 4. JWT_SECRET, DB_PASSWORD 등은 안전한 곳에서 생성
