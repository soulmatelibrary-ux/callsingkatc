# ============================================
# KATC1 유사호출부호 경고시스템 - 환경 설정
# ============================================
# .env.local 파일을 생성하여 아래 값들을 설정하세요
# 주의: .env.local은 .gitignore에 추가되어 커밋되지 않습니다

# ============================================
# 🔑 bkend.ai Configuration (필수)
# ============================================
# https://bkend.ai에서 프로젝트 생성 후 Project ID 입력
NEXT_PUBLIC_BKEND_PROJECT_ID=your_project_id_here

# ============================================
# 📊 Database Configuration
# ============================================
# 로컬 개발 (Docker)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=katc1_dev

# 프로덕션 예시 (AWS RDS)
# DB_HOST=katc1-db.cxxxxxxxxxxxx.us-east-1.rds.amazonaws.com
# DB_USER=katc1admin
# DB_PASSWORD=your_very_secure_password_here

# ============================================
# 🔐 JWT Secret
# ============================================
# 개발환경: 간단한 키 사용 가능
JWT_SECRET=dev_secret_key_12345678_change_in_production

# 프로덕션: 생성 방법 (터미널)
# openssl rand -base64 32
# JWT_SECRET=generate_with_openssl_rand_-base64_32

# ============================================
# 🌐 Application Configuration
# ============================================
NODE_ENV=development
NEXT_PUBLIC_APP_NAME=KATC 유사호출부호 경고시스템

# ============================================
# 📧 Email (SMTP) Configuration
# ============================================
# 옵션 1: AWS SES (프로덕션)
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_USER=your-ses-user
# SMTP_PASSWORD=your-ses-password

# 옵션 2: Gmail (개발)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password (Gmail 앱 비밀번호)

# 옵션 3: 회사 메일서버 (정부기관)
# SMTP_HOST=mail.company.com
# SMTP_PORT=25
# SMTP_USER=no-reply@company.com
# SMTP_PASSWORD=
# SMTP_FROM=no-reply@company.com

# ============================================
# 🚀 선택사항 (프로덕션)
# ============================================
# NEXT_PUBLIC_APP_URL=http://localhost:3000
# LOG_LEVEL=info
# AWS_REGION=us-east-1
# NEXT_PUBLIC_CDN_URL=https://your-cdn-domain.com

# ============================================
# 보안 주의사항
# ============================================
# 1. 민감한 정보는 .env.local에만 저장 (gitignore)
# 2. .env.example은 기본값만 포함 (프로젝트에 커밋)
# 3. 프로덕션에서는 환경변수로 직접 설정
# 4. JWT_SECRET, DB_PASSWORD 등은 안전한 곳에서 생성
